"use strict";

$(document).ready(function () {
  $(".testimonials__slider").slick({
    arrows: true,
    slidesToShow: 3,
    infinite: true,
    speed: 500,
    //autoplay: true,
    autoplaySpeed: 6000,
  });
});
